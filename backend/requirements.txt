# ─────────────────────────────────────────────────────────────────────────────
#  Pro-Rooms Backend — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Core Flask
Flask==3.1.0
Werkzeug==3.1.3
itsdangerous==2.2.0
click==8.1.8
blinker==1.9.0

# Database – PostgreSQL
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
Flask-Migrate==4.0.7

# Authentication
Flask-Login==0.6.3
Flask-JWT-Extended==4.6.0     # NEW: stateless JWT for React frontend

# Google OAuth / SSO
Authlib==1.3.2
requests==2.32.3

# CORS (allows React dev server to call /api/*)
Flask-CORS==4.0.1             # NEW: cross-origin resource sharing

# Environment variables
python-dotenv==1.1.0
